import { colors } from "./colors";
import { space } from "./spacing";
import { lineHeights, fonts, fontSizes, fontWeights } from "./fonts";

export const theme = {
  colors,
  space,
  lineHeights,
  fonts,
  fontSizes,
  fontWeights,
};
